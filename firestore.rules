rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Lock down all client access by default until app-specific rules are defined.
      allow read, write: if false;
    }
  }
}
